[pytest]
testpaths = ui backend
markers =
    ui: drives a real browser through the workstation (needs `playwright install chromium`)
    backend: HTTP and in-process tests, no browser
    slow: takes seconds by design (login throttling, full-library renders)
# Playwright: headed/slowmo are opt-in per run, not baked in here.
addopts = -ra --strict-markers
