# Generated scanner artifacts — reproducible from the pipeline
July01-2027/StockAnalysis_Version1/data/cache/
July01-2027/StockAnalysis_Version1/data/output/

# Secrets — the project .gitignore covers its own .env, but this one sits at
# the repo root and carries live API keys (Resend, Anthropic) plus account size
.env
*.key

# Login store: password hashes, not plaintext, but a committed PBKDF2 hash is
# still an offline cracking target — and it's per-machine state anyway.
July01-2027/StockAnalysis_Version1/data/users.json

# Browser-saved pages that land here by accident, not source
/1.html
/Dashboard · Trading Workstation.html

__pycache__/
*.pyc
